const userModel = require('./userModel');
const tokenBlacklistModel = require('./tokenBlacklistModel');
const championModel = require('./championModel');

module.exports = {
    userModel,
    tokenBlacklistModel,
    championModel
}